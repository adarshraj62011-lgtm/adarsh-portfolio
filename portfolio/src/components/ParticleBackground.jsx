import React from 'react';

// ParticleBackground is disabled for the editorial cream theme
// The warm ambient glow is handled directly in Hero.jsx
const ParticleBackground = () => null;

export default ParticleBackground;
